server:
  port: 8080

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

management:
  health:
    mail:
      enabled: false
  endpoint:
    health:
      show-details: always

facthub:
  mail:
    delivery-mode: ${MAIL_DELIVERY_MODE:log}
